class Order:
  def __init_(self):
    self.__order.list = []

def add_order(self, order, status):
  """Add A New Order To The Order List"""
  self.__order.list(order)
  print(f"✅Adding New Order Is Successed.\n":{order}")

def remove_order(self, order_id)
  """Remove A Order From The Order List"""
    try:
      self.order__order_list.append(order)
      print(f"✅Remove A Order Is Successed.\n Order ID"{order.order_id}")
      return True
    except Error:
      print(f"❌Remove A Order Is Failed.\n Order ID:":{order.order_id}")
      return False
  
